import AIInterviews from "./AIInterviews";

/** `/general` Student Corner: same My Interviews + Performance Analytics hub as `/rvce/interviews`. */
function InterviewAnalyticsPage() {
  return <AIInterviews />;
}

export default InterviewAnalyticsPage;
